[project]
name = "predict-man"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.2",
    "redis>=7.1.0",
    "statbotics>=3.0.0",
    "uvicorn>=0.40.0",
]
